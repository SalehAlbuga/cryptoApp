# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BitPoloniex' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Starscream'
  pod 'RxSwift'
  pod 'RxCocoa'
  # Pods for BitPoloniex
  # noor boora
  target 'BitPoloniexTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BitPoloniexUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
